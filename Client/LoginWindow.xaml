<Window x:Class="HardcoreClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hardcore Messenger - Вход" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">
            
            <!-- Логотип / Заголовок -->
            <TextBlock Text="💬 HARDCORE" 
                       FontSize="32" 
                       FontWeight="Bold"
                       Foreground="#0078D4"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            
            <TextBlock Text="MESSENGER" 
                       FontSize="20" 
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>

            <!-- Табы: Вход / Регистрация -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <RadioButton x:Name="RadioLogin" 
                             Content="ВХОД" 
                             Foreground="White"
                             FontSize="14"
                             FontWeight="SemiBold"
                             IsChecked="True"
                             Margin="0,0,20,0"
                             Checked="RadioLogin_Checked"/>
                <RadioButton x:Name="RadioRegister" 
                             Content="РЕГИСТРАЦИЯ" 
                             Foreground="White"
                             FontSize="14"
                             FontWeight="SemiBold"
                             Checked="RadioRegister_Checked"/>
            </StackPanel>

            <!-- Поля ввода -->
            <TextBlock Text="Имя пользователя" 
                       Foreground="#AAAAAA" 
                       FontSize="12" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="TxtUsername" 
                     Style="{StaticResource ModernTextBox}"
                     Margin="0,0,0,15"/>

            <TextBlock Text="Пароль" 
                       Foreground="#AAAAAA" 
                       FontSize="12" 
                       Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPassword" 
                         Style="{StaticResource ModernPasswordBox}"
                         Margin="0,0,0,20"/>

            <!-- Статус / Ошибки -->
            <TextBlock x:Name="TxtStatus" 
                       Foreground="#FF5555"
                       FontSize="12"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"
                       Visibility="Collapsed"/>

            <!-- Кнопка входа/регистрации -->
            <Button x:Name="BtnSubmit" 
                    Content="ВОЙТИ" 
                    Style="{StaticResource ModernButton}"
                    Click="BtnSubmit_Click"
                    Height="45"/>

            <!-- Индикатор загрузки -->
            <ProgressBar x:Name="LoadingBar" 
                         IsIndeterminate="True"
                         Height="3"
                         Margin="0,15,0,0"
                         Foreground="#0078D4"
                         Background="Transparent"
                         Visibility="Collapsed"/>

            <!-- Информация о сервере -->
            <TextBlock x:Name="TxtServerInfo" 
                       Text="Подключение к серверу..."
                       Foreground="#666666"
                       FontSize="10"
                       HorizontalAlignment="Center"
                       Margin="0,30,0,0"/>

        </StackPanel>
    </Grid>
</Window>
